# ── Build artifacts ──
.next/
out/
node_modules/
tsconfig.tsbuildinfo

# ── Backup / copy files (MUST NOT be deployed) ──
src - Copy/
src.zip
webcode.zip
*.bak
*- Copy*
*Copy*

# ── Data backups ──
src/data/projects - Copy*.json
src/data/projects - Copy*.txt
src/data/projects*.bak
src/data/projects1*.json

# ── Nested duplicate components ──
src/components/src/

# ── Environment ──
# IMPORTANT: .env.local is included for convenience but should
# be set as Environment Variables in Vercel dashboard for production.
# Don't commit to public repos with real secrets.
.env
.env.*.local

# ── OS ──
.DS_Store
Thumbs.db

# ── Editor ──
.vscode/
.idea/
