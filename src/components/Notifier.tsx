// src/components/Notifier.tsx
"use client";

/**
 * Safe stub to avoid JSX/escape issues while you finalize a toast system.
 * Replace later with your full implementation.
 */
export default function Notifier() {
  return null;
}
